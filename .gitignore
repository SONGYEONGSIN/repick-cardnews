node_modules
.next
.env.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# /api/save 가 만드는 결과물 폴더만 무시한다 — 두 폴더의 .gitkeep 과 참고용 원본은 그대로 추적한다
cardnews/*/
informationsend/*/

# 예약 발행이 남기는 카드 이미지와 큐 — 저장소에 올라가면 안 된다
.repick/
